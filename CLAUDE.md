# CLAUDE.md

이 프로젝트의 작업 규범. 글로벌 `~/.claude/CLAUDE.md`와 메모리(`MEMORY.md`)를 보완한다.

---

## 1. 말투·문체

- 응답과 산출 문서는 표준 문어체로 작성한다. 일상 구어체 동사, 인터넷 줄임말·신조어, 외래어 직역(픽커·페이로드·라벨 등)을 지양한다.
- 매뉴얼 본문 콘텐츠 용어는 한국어 공식 docs SSOT를 따른다. 메모리 `feedback_official_terminology` 참조.
- 사용자 보고와 핸드오프는 같은 기준을 적용한다.

## 2. 업데이트 루틴 진입점

- 기능·릴리스 반영 작업은 루트 `UPDATE_PLAYBOOK.md`의 단계대로 진행한다.
- 출처 검증은 메모리 `reference_anthropic_sources`·`reference_openai_codex_sources`를 1순위로 활용한다.
- "조사해서 추가해"라는 자유 프롬프트로 진입할 경우에도 UPDATE_PLAYBOOK §2 단계 0~5의 순서를 그대로 따른다.

## 3. 핵심 보존 영역

- 매뉴얼 본문 16편의 인라인 `<style>` 블록은 변경 금지. 색조·다크 모드 변경은 `_shared/manual-theme.css`에서 `[data-theme="dark"]` override로 처리한다.
- `localStorage`의 `theme` 단일 키로 사이트 전체(메인 2편 + 매뉴얼 허브 2편 + 본문 16편 = 20개 페이지) 모드를 동기화한다. 새 페이지를 추가할 때 동일 키를 사용한다.
- `_shared/cmd-detail.{css,js}`의 셀렉터·동작은 플레이북 12편이 의존하므로 계약을 유지한다.
- 6단계 액센트 토큰 `--c1`~`--c6c`의 색조와 의미는 보존한다 (6번 변수명만 `--c6c`인 점 주의).

## 4. 작업 원칙

- 동일 패턴을 N편에 일괄 적용할 때는 grep으로 패턴 단일성을 사전 검증한다. 패턴이 분기되면 분기별로 처리한다 (다크모드 사이클의 16편 동일 head·body 패턴 → 32 Edit 일괄 처리 사례).
- 핸드오프·기존 문서가 "X 키 통일됨"이라 단언해도 실제 상태와 다를 수 있다. 핵심 상수·키·셀렉터는 grep으로 직접 재확인한 뒤 작업한다.
- UI 동작 검증은 브라우저 더블클릭이 필수다. 타입 체크·테스트 통과는 기능 완성을 의미하지 않는다. 검증하지 못한 부분은 보고에서 명시한다.
- 본문에 인라인 `style="..."`로 색상이 지정된 경우 외부 CSS override로 잡히지 않는다. `!important`로 우회하거나 본문 직접 수정이 필요하다(§3 보존 원칙의 예외 케이스).

---

*세부 업데이트 절차와 WATCHLIST·파일 매핑·교훈은 루트 `UPDATE_PLAYBOOK.md` 참조.*
